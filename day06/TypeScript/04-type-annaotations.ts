/**
 * Type Annoations = Explicit type inference
 * annotating/mentioning the type for the variable
 * 
 */

function addTwoNumbers(x:number, y:number):number {
    return x+y;
}

console.log(addTwoNumbers(5,10));
